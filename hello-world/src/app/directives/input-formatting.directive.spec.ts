import { InputFormattingDirective } from './input-formatting.directive';

describe('InputFormattingDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFormattingDirective();
    expect(directive).toBeTruthy();
  });
});
